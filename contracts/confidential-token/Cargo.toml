[package]
name = "confidential-token"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = { workspace = true }
fhe-lib = { path = "../fhe-lib" }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
